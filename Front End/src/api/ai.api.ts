import {api} from './client';import type {ApiResponse} from '../types';export async function generateWorkflow(requirements:string){const {data}=await api.post<ApiResponse<unknown>>('/ai/generate-workflow',{requirements});return data.data}export async function analyzeFailure(runId:string,question:string){const {data}=await api.post<ApiResponse<unknown>>(`/ai/analyze-failure/${runId}`,{question});return data.data}