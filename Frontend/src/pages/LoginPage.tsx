import {useState} from 'react';import {login,register} from '../api/auth.api';import {useAuth} from '../stores/auth.store';
export function LoginPage(){const setSession=useAuth(s=>s.setSession);const [email,setEmail]=useState('admin@example.com');const [password,setPassword]=useState('Password123');const [name,setName]=useState('Admin');const [tenant,setTenant]=useState('Demo Tenant');const [error,setError]=useState('');async function submit(mode:'login'|'register'){try{const res=mode==='login'?await login({email,password}):await register({tenant_name:tenant,tenant_slug:tenant.toLowerCase().split(' ').join('-'),name,email,password});setSession(res)}catch(e:any){setError(e.response?.data?.error?.message||e.message)}}return <div className="panel"><h1>FlowForge Login</h1><input value={tenant} onChange={e=>setTenant(e.target.value)} placeholder="Tenant"/><input value={name} onChange={e=>setName(e.target.value)} placeholder="Name"/><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input value={password} onChange={e=>setPassword(e.target.value)} type="password"/><button onClick={()=>submit('login')}>Login</button><button onClick={()=>submit('register')}>Register</button><p className="error">{error}</p></div>}
